<%- include('../partials/_head') %>
    <div class="container">
        <div class="rows">
            <div class="col">
                <a href="/penyimpanan" class="btn btn-success btn-sm mb-3">Kembali</a>
                <form action="/penyimpanan/store" method="post">
                    <input type="text" placeholder="nama penyimpanan" class="form-controll d-block mb-3" name="nama_penyimpanan" required>
                    <input type="text" placeholder="kode penyimpanan" class="form-controll d-block mb-3" name="kode_penyimpanan" required>
                    <select name="id_ruangan" class="d-block mb-3">
                        <% for( let i = 0; i < data.length; i++ ) { %>
                            <option value="<%= data[i].id_ruangan %>"><%= data[i].nama_ruangan %></option>
                        <% } %>
                    </select>
                    <button class="btn btn-primary btn-sm">submit</button>
                </form>
            </div>
        </div>
    </div>
<%- include('../partials/_footer') %>